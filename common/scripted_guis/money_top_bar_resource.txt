scripted_gui = {

	money = {
		context_type = player_context
		parent_window_token = top_bar
		window_name = "money_container"
		visible = {
            always=yes
		}

		effects = {
            buy_icon_click = {
				 if = {
           limit={
                check_variable = { show = 0 }
           }
           set_variable = { show = 1 }
           else ={
           set_variable = { show = 0 }
           }
           }
			}
		}
		
		dynamic_lists = {
			grid_example = {
				array = grid_array
				entry_container = grid_entry
			}
		}

		properties = {
			flag = {
				image = "[This.GetFlag]"
			}
		}
	}
	
	flag = {
		window_name = "grid_container"
		#context_type = player_context
		#parent_window_token = top_bar
		visible = {
            always=yes
		}	
		dynamic_lists = {
			grid_example = {
				array = grid_array
				entry_container = grid_entry
			}
		}

		properties = {
			flag = {
				image = "[This.GetFlag]"
			}
		}
    
}

